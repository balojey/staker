{
  "name": "staker-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "staker-frontend",
    "./"
  ],
  "scripts": {
    "dev:ui": "npm run dev --workspace=staker-frontend",
    "test:program": "npm test --workspace=.",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
